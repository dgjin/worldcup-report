# football-data.org 免费 API token（可选）
# 去 https://www.football-data.org/client/register 1 分钟注册即可拿到免费 key
# 不填也能跑：后端会自动回退到 data/snapshot.json 快照数据
FOOTBALL_DATA_TOKEN=

# 服务端口（可选，默认 5273）
PORT=5273

# Supabase 数据库（可选，不填则回退到快照数据）
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
